Instructions to use server:

Configuration:

1) Make sure to create a db called tictactoe - can use file in folder.

2) Make sure to write the detailes in file in folder index.ts src/database
PORT=server port
DB_NAME = DB name
DB_USERNAME = DB user name
DB_PASSWORD = DB user Password
DB_HOST= server address
also if need change dialect if using differnt database than MySQL

Activition:

4) cmd: get to root folder of project. using cd to folder.

5) cmd: npm install

6) cmd: npm start
